# Server
fastapi
uvicorn[standard]
python-multipart
websockets

# Speech to text. faster-whisper pulls ctranslate2, onnxruntime (for Silero
# VAD), av and numpy. No whisper.cpp, no separate server process.
faster-whisper>=1.1

# Answering path. BACKEND=api uses this SDK; BACKEND=cli drives the `claude`
# binary as a subprocess and needs nothing extra. The SDK is installed either
# way -- it is small, and the default backend is api.
anthropic>=0.40

# The system-wide hotkey.
pynput
