# No key needed by default. Drives the `claude` CLI headlessly on your
# existing subscription login. Must be logged in already (run `claude` once).
CLAUDE_MODEL=sonnet
CLAUDE_BIN=claude

# Answers stay in the CLI session's context; it's recycled every N turns so
# earlier answers don't anchor later ones. Costs one warmup turn each time.
RECYCLE_AFTER=8

# Audio input index from: ffmpeg -f avfoundation -list_devices true -i ""
AUDIO_DEVICE=1

# How far back the trigger looks, in seconds.
WINDOW_SECONDS=90
CHUNK_SECONDS=3

WHISPER_MODEL=models/ggml-large-v3-turbo.bin
HOTKEY_ENABLED=1
PORT=8477
